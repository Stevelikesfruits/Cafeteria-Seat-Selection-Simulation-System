# 图1a：项目总览 — 分层架构图

```mermaid
graph TD
    %% ==================== 样式定义 ====================
    classDef uiLayer fill:#2979ff,color:#fff,stroke:#1a5acc,stroke-width:2px
    classDef coreLayer fill:#00BFFF,color:#fff,stroke:#0099cc,stroke-width:2px
    classDef modelLayer fill:#4A70C4,color:#fff,stroke:#2d4a8e,stroke-width:2px
    classDef entry fill:#FFA500,color:#333,stroke:#cc8400,stroke-width:2px
    classDef signal fill:#FF6B6B,color:#fff,stroke:#cc5555,stroke-width:1px,stroke-dasharray:5 5
    classDef data fill:#8FBC8F,color:#333,stroke:#6a9a6a,stroke-width:1px

    %% ==================== 入口层 ====================
    subgraph Entry [入口层 — main_ui.py]
        direction LR
        ENTRY["🐍 main_ui.py<br/>QApplication 启动<br/>创建 MainWindow"]
        TEST["🧪 main_test.py<br/>脱机后端测试<br/>60分钟纯引擎运行"]
    end

    %% ==================== UI 层 ====================
    subgraph UI [UI 层 — ui/]
        direction TB

        subgraph UI_Main [主窗口]
            MW["MainWindow<br/>━━━━━━━━━━━<br/>定时器驱动仿真<br/>按钮: 开始/暂停/结束<br/>持有 → engine"]
        end

        subgraph UI_Components [UI 组件]
            TD["TableCountDialog<br/>━━━━━━━━━━━<br/>弹窗输入桌数<br/>校验 ≤200 空间"]
            CP["ControlPanel<br/>━━━━━━━━━━━<br/>4个偏好%输入框<br/>饼图实时预览<br/>━━━━━━━━━━━<br/>📡 Signal: preferences_changed"]
            RV["RestaurantView<br/>━━━━━━━━━━━<br/>QGraphicsView 画布<br/>绘制桌椅/座位颜色<br/>按 Table 类型布局"]
            PC["PreferencePieChart<br/>━━━━━━━━━━━<br/>matplotlib 饼图<br/>嵌入 Qt 画布"]
        end
    end

    %% ==================== Core 层 ====================
    subgraph Core [Core 层 — core/]
        direction TB
        SE["SimulationEngine<br/>━━━━━━━━━━━<br/>🕐 仿真主循环 step()<br/>📍 管理时间线<br/>📊 提供统计数据<br/>━━━━━━━━━━━<br/>持有: Restaurant<br/>持有: StudentGenerator<br/>持有: SeatAllocator"]

        subgraph Core_Modules [Core 子模块]
            SG["StudentGenerator<br/>━━━━━━━━━━━<br/>倒U型人流: y=-0.05(t-30)²+15<br/>按 pref_ratios 分配偏好"]
            SA["SeatAllocator<br/>━━━━━━━━━━━<br/>1️⃣ 完美匹配 _find_perfect_match<br/>2️⃣ 降级查找 _find_any_free_seat<br/>遍历每张桌子→检查每个座位"]
            SC["SatisfactionCalculator<br/>━━━━━━━━━━━<br/>纯静态方法<br/>+2 完美 / +1 降级 / -1 未落座"]
        end
    end

    %% ==================== Models 层 ====================
    subgraph Models [Models 层 — models/]
        direction TB
        PT["PreferenceType 枚举<br/>━━━━━━━━━━━<br/>SINGLE / FACE_TO_FACE<br/>DIAGONAL / ADJACENT"]

        ST["Student 数据类<br/>━━━━━━━━━━━<br/>id / preference / arrival_time<br/>seated_table_id / seat_indices<br/>leave_time / satisfaction_score<br/>━━━━━━━━━━━<br/>🌟 最常用: id, preference"]

        subgraph Tables [桌子体系]
            T["Table 基类<br/>━━━━━━━━━━━<br/>id / capacity / space_cost<br/>🌟 seats: np.array ← 核心数据<br/>get_free_seats() / is_full"]
            T2["Table2 双人桌<br/>capacity=2 space=4"]
            T4["Table4 四人桌<br/>capacity=4 space=6"]
        end

        R["Restaurant 容器<br/>━━━━━━━━━━━<br/>tables: dict(int→Table)<br/>total_space=200<br/>🌟 最常用: tables 字典"]
    end

    %% ==================== 数据流 — 启动阶段 ====================
    ENTRY -->|① 创建实例| MW
    MW -->|② 弹窗取桌数| TD
    TD -->|③ num_2, num_4| MW
    MW -->|④ 传入桌数| SE
    SE -->|⑤ 创建| R
    SE -->|⑥ 创建| SG
    SE -->|⑦ 创建 + 传入R| SA
    MW -->|⑧ init_ui| CP
    MW -->|⑨ init_restaurant_layout| RV

    %% ==================== 数据流 — 运行时每Tick ====================
    MW -->|⏱️ QTimer 每500ms触发| SE
    SE -->|step: ① departures 清理离店| SE
    SE -->|② generate_batch| SG
    SG -->|③ 返回 List| SE
    SE -->|④ allocate| SA
    SA -->|⑤ 写入 seats| R
    SE -->|⑥ calculate| SC
    SC -->|⑦ 写入 satisfaction_score| ST
    SE -->|⑧ update_view| RV
    RV -->|⑨ 读取 tables| R

    %% ==================== 信号/槽通道 ====================
    CP -.->|📡 Signal: preferences_changed<br/>连接 → engine.update_preferences| SE
    CP -->|输入变化 → update_chart| PC

    %% ==================== 测试通道 ====================
    TEST -->|直接调用 engine.step ×60次| SE

    %% ==================== 类引用依赖 ====================
    SG -.->|引用| PT
    SG -.->|创建实例| ST
    SA -.->|引用| PT
    SA -.->|读取属性| ST
    SA -.->|遍历查找| R
    SA -.->|类型判断| T2
    SA -.->|类型判断| T4
    SC -.->|写入分数| ST

    %% ==================== 样式应用 ====================
    class ENTRY,TEST entry
    class MW,TD,CP,RV,PC uiLayer
    class SE,SG,SA,SC coreLayer
    class PT,ST,T,T2,T4,R modelLayer
```

---

## 关键数据流总结

| 阶段 | 流向 | 传递内容 |
|------|------|----------|
| 启动 | UI → Core | `num_tables_2: int`, `num_tables_4: int` |
| 运行时配置 | ControlPanel → SimulationEngine | `pref_ratios: Dict[PreferenceType, float]` (通过 Qt Signal) |
| 每tick:生成 | SimulationEngine → StudentGenerator | `current_time: int`, `pref_ratios: dict` |
| 每tick:生成 | StudentGenerator → SimulationEngine | `List[Student]` |
| 每tick:分配 | SimulationEngine → SeatAllocator | `Student` 对象 |
| 每tick:分配 | SeatAllocator → Restaurant | 写入 `table.seats[i] = student.id` |
| 每tick:满意度 | SimulationEngine → SatisfactionCalculator | `Student` 对象 + `is_perfect_match: bool` |
| 每tick:渲染 | MainWindow → RestaurantView | `Restaurant` 对象 (只读 tables) |
| 结束 | SimulationEngine → MainWindow | `get_statistics(): dict` |

## 核心接口一览

| 接口 | 类型 | 描述 |
|------|------|------|
| `SimulationEngine(pref_ratios) ← ControlPanel` | Qt Signal/Slot | 唯一运行时用户输入通道 |
| `SimulationEngine.step()` | 方法调用 | 每 tick 的主入口 |
| `SimulationEngine.get_statistics()` | 方法调用 | 获取当前统计数据 |
| `SeatAllocator.allocate(student) → bool` | 方法调用 | 座位分配核心接口 |
| `StudentGenerator.generate_batch(time, ratios) → List[Student]` | 方法调用 | 学生生成接口 |
| `RestaurantView.update_view(restaurant)` | 方法调用 | 视图刷新接口 |
